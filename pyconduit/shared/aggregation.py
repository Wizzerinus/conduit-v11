from pyconduit.models.bundle import Aggregator
from pyconduit.models.conduit import ConduitContent


def build_aggregator(filter_data: Aggregator, documents: dict[str, ConduitContent]) -> ConduitContent:
    # TODO: write this
    pass
